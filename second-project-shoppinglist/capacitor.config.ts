import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Project Market List',
  webDir: 'www.themarto.es'
};

export default config;
